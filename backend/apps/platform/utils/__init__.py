"""
Platform utilities package.

Helper functions and utilities for the platform application.

Modules:
    settings: Platform settings helpers (get_setting, is_maintenance_mode)
    features: Feature flag resolution and caching (low-level)
    flags: Feature flag helpers (is_enabled, get_flag, require_feature)
"""
